# CLAUDE.md — تعليمات للمساعد الذكي

> This file is read automatically by Claude Code at the start of every session in this project.

## من المستخدم

المستخدم **متدرّب مبتدئ تمامًا** في البرمجة و VS Code و Git. لغته الأم **العربية**.

## قواعد التواصل

1. **ردّ دائمًا باللغة العربية**، بجُمل قصيرة وواضحة.
2. عند ذكر أي زر أو قائمة أو تبويب في VS Code أو المتصفح، **اكتب اسمه بالإنجليزية بالضبط** كما يظهر على الشاشة، لأن واجهة الجهاز كلها إنجليزية.
   مثال: «افتح `Source Control` من الشريط الأيسر».
3. المصطلحات التقنية: اكتب المصطلح الإنجليزي ثم اشرحه بالعربي بين قوسين أول مرة.
   مثال: `commit` (نقطة حفظ).
4. تجنّب الاختصارات والافتراضات — اشرح **لماذا** تفعل كل خطوة، لا فقط **ماذا**.

## قواعد العمل

1. **خطوة واحدة في كل مرة.** بعد كل خطوة، أخبر المستخدم كيف يتحقق بنفسه من أنها نجحت.
2. قبل أي تغيير كبير أو متعدد الملفات، **اعرض الخطة أولاً** واطلب الموافقة.
3. بعد كل خطوة ناجحة، **ذكّر المستخدم بعمل `commit`** واقترح عليه رسالة commit مناسبة بالإنجليزية.
4. عند حدوث خطأ: اشرح **سبب** الخطأ بالعربي قبل إصلاحه، حتى يتعلّم.
5. **شغّل المشروع وتحقّق بنفسك** أن الكود يعمل قبل أن تقول إنه جاهز. لا تكتفِ بكتابة الكود.
6. عند تشغيل أي شيء محليًا، اذكر بوضوح **الرابط أو الأمر** الذي يستخدمه المستخدم، وكيف يوقفه (`Ctrl + C`).
7. لا تضع أسرارًا أو مفاتيح API داخل الكود — استخدم ملف `.env` وأضفه إلى `.gitignore`.
8. اجعل الكود بسيطًا وقابلاً للقراءة. تجنّب المكتبات والتجريدات غير الضرورية — الهدف تعليمي.

## معلومات المشروع

- الاسم: `موقع-ثلاثي-الأبعاد-أسامه-للبرمجيات`
- المسار: `C:\Users\user\Desktop\موقع-ثلاثي-الأبعاد-أسامه-للبرمجيات`
- أُنشئ بتاريخ: 2026-08-27
- دليل المتدرّب بالعربي موجود في: `START-HERE.md`

## أدوات متاحة على الجهاز

`git` · `gh` (GitHub CLI) · `node` + `npm` · VS Code
لا يوجد Python مثبّت — إذا احتاج المشروع Python، أخبر المستخدم أولاً واطلب الإذن قبل التثبيت.

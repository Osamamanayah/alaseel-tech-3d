# 👋 أهلاً بك في مشروع: موقع-ثلاثي-الأبعاد-أسامه-للبرمجيات

> **هذا الملف هو دليلك الكامل. اقرأه مرة واحدة بالكامل، ثم ارجع إليه كلما احتجت.**
> مكان المشروع على جهازك: `C:\Users\user\Desktop\موقع-ثلاثي-الأبعاد-أسامه-للبرمجيات`
> تاريخ الإنشاء: 2026-08-27

⚠️ **ملاحظة مهمة:** واجهة الكمبيوتر والبرامج كلها **بالإنجليزية**.
لذلك كل زر أو قائمة سأكتبها **بالإنجليزية بين علامتين** مثل `Source Control` — ابحث عن الكلمة الإنجليزية بالضبط على الشاشة.

---

## 📋 أول ٥ دقائق — افعل هذا بالترتيب

1. **لا تغلق هذا الملف.** أبقه مفتوحًا في الخلفية.
2. انظر إلى **يمين الشاشة** — ستجد لوحة الذكاء الاصطناعي (`Claude Code` و `Codex`).
   إذا لم تظهر: اضغط `Ctrl + Alt + B` أو من الأعلى `View` ← `Appearance` ← `Secondary Side Bar`.
3. اضغط داخل مربع الكتابة في `Claude Code` واكتب أول رسالة:
   ```
   اشرح لي بالعربي ماذا يوجد في هذا المشروع الآن، وما هي الخطوة التالية.
   ```
4. اقرأ الرد. إذا لم تفهم كلمة — اسأله: `اشرح لي كلمة كذا ببساطة`.
5. أكمل قراءة هذا الملف.

---

## 🗺️ جولة في شاشة VS Code

الشريط الطويل على **يسار الشاشة** اسمه `Activity Bar`. هذه أهم أيقوناته:

| الأيقونة / الاسم الإنجليزي | ماذا تفعل | الاختصار |
|---|---|---|
| `Explorer` | تعرض كل ملفات ومجلدات المشروع | `Ctrl + Shift + E` |
| `Search` | تبحث عن أي كلمة داخل كل الملفات | `Ctrl + Shift + F` |
| `Source Control` | من هنا تحفظ نسخة من عملك وترفعها على GitHub | `Ctrl + Shift + G` |
| `Extensions` | لتثبيت إضافات جديدة | `Ctrl + Shift + X` |

مناطق أخرى مهمة:

- **`Terminal`** (الطرفية): نافذة سوداء في الأسفل تكتب فيها الأوامر.
  تفتحها بـ `` Ctrl + ` `` (زر الباك-تيك، تحت زر Esc)، أو من `View` ← `Terminal`.
- **`Command Palette`**: صندوق أوامر VS Code كلها. اضغط `Ctrl + Shift + P` واكتب اسم أي أمر.
- **`Secondary Side Bar`** (يمين الشاشة): مكان `Claude Code` و `Codex`.

### أهم قاعدتين في VS Code

1. **احفظ دائمًا** بـ `Ctrl + S`. الملف الذي لم يُحفظ تظهر بجانب اسمه **دائرة بيضاء ⚪** بدل علامة ×.
2. **الملف ذو الاسم المائل** في الأعلى = معاينة مؤقتة. اضغط **دبل كليك** على اسم الملف في `Explorer` لتثبيته.

---

## 🤖 كيف تطلب التطوير من Claude

`Claude Code` يقرأ ملفات مشروعك ويكتب الكود ويشغّله نيابةً عنك. **تكلم معه بالعربي بشكل طبيعي.**

### كيف تفتحه
- **الطريقة الأسهل:** اضغط على أيقونة `Claude Code` في الشريط على **يمين** الشاشة.
- أو اضغط `Ctrl + Esc`.
- أو `Ctrl + Shift + P` ← اكتب `Claude Code: Open` ← `Enter`.

### القاعدة الذهبية للطلب: **ماذا + أين + كيف أعرف أنه نجح**

❌ **طلب سيء:**
```
سوّي لي موقع
```

✅ **طلب جيد:**
```
أريد صفحة ويب بسيطة فيها:
- عنوان في الأعلى مكتوب فيه "متجر أحمد"
- ثلاثة صناديق منتجات، كل صندوق فيه صورة واسم وسعر
- زر "اشترِ الآن" تحت كل منتج
استخدم HTML و CSS فقط بدون مكتبات.
وبعد ما تخلص، شغّل الموقع وقل لي كيف أفتحه في المتصفح.
```

### مهارات تستخدمها كل يوم

| ما تريد فعله | كيف |
|---|---|
| الإشارة إلى ملف معيّن | اكتب `@` ثم ابدأ بكتابة اسم الملف واخترْه من القائمة |
| تخطيط قبل التنفيذ | اضغط `Shift + Tab` لتفعيل **`plan mode`** — سيشرح الخطة قبل أن يكتب أي كود |
| بدء محادثة جديدة | زر `+` في أعلى لوحة Claude — افعلها عند تغيير الموضوع |
| إيقافه في المنتصف | اضغط `Esc` |
| مراجعة ما غيّره | افتح `Source Control` وشاهد الملفات المتغيّرة |

### 💡 جُمل جاهزة انسخها واستخدمها

```
اشرح لي هذا الملف سطرًا سطرًا بالعربي.
```
```
لا تكتب كود الآن. اشرح لي الخطة أولاً وانتظر موافقتي.
```
```
شغّل المشروع محليًا وقل لي الرابط الذي أفتحه.
```
```
ظهر لي هذا الخطأ، حلّه واشرح لي سبب حدوثه:
<الصق نص الخطأ هنا>
```
```
راجع الكود الذي كتبته وتأكد أنه يعمل فعلاً، ثم اختبره.
```

---

## 🌸 Codex — رأي ثانٍ

`Codex` مساعد ذكاء اصطناعي آخر (من OpenAI) موجود في نفس الشريط الأيمن.

- تفتحه بالضغط على أيقونة `Codex` في **يمين** الشاشة.
- أول مرة سيطلب منك تسجيل الدخول: اضغط `Sign in` واستخدم حساب ChatGPT.

**متى تستخدم أيّهما؟**
- **Claude**: للعمل الأساسي — بناء المشروع، تعديل عدة ملفات، تشغيل الأوامر.
- **Codex**: عندما يعلق Claude في مشكلة، أو لتأخذ رأيًا ثانيًا. انسخ المشكلة واسأل Codex عنها.

> نصيحة: لا تشغّل الاثنين على **نفس الملف** في نفس الوقت — سيتعارضان.

---

## ▶️ كيف تشغّل المشروع على جهازك (Run Locally)

**أسهل طريقة:** اطلب من Claude:
```
شغّل المشروع محليًا وقل لي ماذا أفتح.
```

### وإذا أردت أن تفعلها بنفسك

افتح `Terminal` بـ `` Ctrl + ` `` ثم:

**مشروع ويب (Node / React / Next):**
```
npm install
npm run dev
```

**صفحة HTML بسيطة:**
اضغط **كليك يمين** على ملف `index.html` في `Explorer` ← اختر `Reveal in File Explorer` ← دبل كليك عليه.

### قراءة نتيجة التشغيل

- إذا ظهر سطر مثل `Local: http://localhost:3000` → اضغط `Ctrl` + كليك على الرابط ليُفتح في المتصفح.
- **لإيقاف التشغيل:** اضغط داخل `Terminal` ثم `Ctrl + C`.
- **لتنظيف الشاشة:** اكتب `cls` واضغط `Enter`.

---

## 🔗 Git و GitHub — حفظ عملك للأبد

### المصطلحات بأبسط شكل

| المصطلح | المعنى بالعربي |
|---|---|
| `repository` (repo) | مستودع = مجلد المشروع مع كل تاريخ تعديلاته |
| `commit` | **نقطة حفظ** — لقطة من المشروع في لحظة معينة مع وصف لما تغيّر |
| `push` | **رفع** نقاط الحفظ من جهازك إلى GitHub |
| `pull` | **تنزيل** آخر التعديلات من GitHub إلى جهازك |
| `branch` | فرع — نسخة جانبية تجرّب فيها بأمان |

### 🔄 الروتين اليومي (احفظه)

كلما أنهيت خطوة تعمل بشكل صحيح:

1. افتح `Source Control` من الشريط الأيسر (`Ctrl + Shift + G`).
2. سترى قائمة الملفات المتغيّرة. مرّر بالماوس فوق `Changes` واضغط علامة **`+`** (`Stage All Changes`).
3. اكتب في المربع العلوي وصفًا قصيرًا لما فعلته، مثال: `added product cards`.
4. اضغط زر **`Commit`**.
5. اضغط زر **`Sync Changes`** لرفع التعديلات إلى GitHub.

> ⚠️ **اعمل `commit` كثيرًا** — بعد كل خطوة صغيرة ناجحة، وليس مرة واحدة في نهاية اليوم.
> نقطة الحفظ هي طريق العودة إذا خرب شيء.

### الطريقة عبر الأوامر (نفس الشيء)

```
git add .
git commit -m "وصف قصير لما فعلته"
git push
```

### ربط المشروع بـ GitHub لأول مرة

إذا لم تختر إنشاء المستودع عند إنشاء المشروع، افتح `Terminal` واكتب:

```
gh auth login
```
اختر: `GitHub.com` ← `HTTPS` ← `Y` ← `Login with a web browser`، وانسخ الرمز الظاهر والصقه في المتصفح.

ثم أنشئ المستودع وارفع المشروع بأمر واحد:
```
gh repo create موقع-ثلاثي-الأبعاد-أسامه-للبرمجيات --private --source=. --remote=origin --push
```

> `--private` تعني أن المستودع **خاص** ولا يراه أحد غيرك. استبدلها بـ `--public` إذا أردته علنيًا.

أو ببساطة اطلب من Claude:
```
اربط هذا المشروع بمستودع GitHub خاص وارفعه.
```

---

## 🛠️ حل المشاكل — الطريقة الصحيحة

### 🥇 القاعدة رقم ١: انسخ الخطأ كاملاً والصقه لـ Claude

**لا تعيد كتابة الخطأ بكلماتك.** حدّد نص الخطأ في `Terminal` بالماوس، انسخه بـ `Ctrl + C`، والصقه:

```
ظهر لي هذا الخطأ، اشرح لي سببه بالعربي ثم أصلحه:

<الصق الخطأ هنا>
```

### إذا كانت المشكلة في المتصفح

اضغط `F12` في المتصفح ← افتح تبويب `Console` ← انسخ النص **الأحمر** والصقه لـ Claude.

### 📊 جدول المشاكل الشائعة

| ما تراه | المعنى | الحل |
|---|---|---|
| `command not found` أو `is not recognized` | البرنامج غير مثبّت أو الأمر مكتوب خطأ | اطلب من Claude: `هذا الأمر لا يعمل، ماذا أفعل؟` |
| `EADDRINUSE` / `port already in use` | المنفذ مشغول بتشغيل سابق | اضغط `Ctrl + C` في الـ `Terminal` القديم، أو اطلب منفذًا آخر |
| `Cannot find module` | حزمة ناقصة | اكتب `npm install` |
| `permission denied` | لا تملك صلاحية | أخبر Claude بالرسالة كاملة |
| الصفحة بيضاء في المتصفح | خطأ في الكود | `F12` ← `Console` ← انسخ الأحمر |

### 🔙 إذا خرب كل شيء — كيف ترجع للخلف

**إذا لم تعمل `commit` بعد:**
`Source Control` ← كليك يمين على الملف ← `Discard Changes` (يرجّع الملف لآخر نقطة حفظ).

**عبر الأوامر:**
```
git restore .
```

**إذا أردت الرجوع لنقطة حفظ سابقة:** لا تفعلها يدويًا — اطلب من Claude:
```
أريد الرجوع إلى آخر commit كان يعمل. ساعدني خطوة بخطوة.
```

### ❌ أشياء لا تفعلها أبدًا

- لا تحذف مجلد `.git` — ستفقد كل تاريخ المشروع.
- لا تحذف مجلدات يدويًا من `File Explorer` — اطلب من Claude.
- لا تكتب أوامر نسختها من الإنترنت دون أن تفهمها — الصقها لـ Claude واسأل: `ماذا يفعل هذا الأمر؟`
- لا تضع كلمات سر أو مفاتيح API داخل الكود — ضعها في ملف `.env` (اسأل Claude كيف).

---

## ✅ عادات المطور الناجح

1. **خطوة صغيرة واحدة في كل مرة.** لا تطلب عشرة أشياء في رسالة واحدة.
2. **جرّب بعد كل خطوة.** شغّل المشروع وتأكد أنه يعمل قبل الانتقال للتالي.
3. **`commit` بعد كل نجاح.**
4. **إذا لم تفهم — اسأل.** اكتب: `اشرح لي هذا ببساطة كأنني مبتدئ تمامًا.`
5. **اقرأ ما يكتبه Claude** قبل أن تضغط موافق. أنت المسؤول عن مشروعك.
6. **الخطأ ليس فشلاً** — كل مطور يقضي معظم وقته في حل الأخطاء. هذا هو العمل نفسه.

---

## ⌨️ أهم الاختصارات

| الاختصار | الوظيفة |
|---|---|
| `Ctrl + S` | حفظ الملف |
| `Ctrl + Esc` | فتح / إغلاق `Claude Code` |
| `Ctrl + Alt + B` | إظهار / إخفاء الشريط الأيمن (`Secondary Side Bar`) |
| `` Ctrl + ` `` | فتح / إغلاق `Terminal` |
| `Ctrl + Shift + P` | `Command Palette` — كل الأوامر |
| `Ctrl + Shift + G` | `Source Control` |
| `Ctrl + Shift + E` | `Explorer` |
| `Ctrl + P` | فتح ملف بالبحث عن اسمه |
| `Ctrl + Z` | تراجع |
| `Ctrl + C` (في الـ Terminal) | إيقاف التشغيل |
| `Ctrl + /` | تعليق / إلغاء تعليق السطر |

---

## 🚀 ابدأ الآن

انسخ هذه الرسالة والصقها في `Claude Code`:

```
أنا مبتدئ تمامًا. اشرح لي بالعربي ماذا يوجد في هذا المشروع،
ثم اسألني ماذا أريد أن أبني، وساعدني خطوة بخطوة.
```

**بالتوفيق! 🎉**
